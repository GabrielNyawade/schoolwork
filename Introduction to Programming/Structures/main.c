#include <stdio.h>
#include <stdlib.h>

struct StudentData
{
    int stud_no;
    char name[20];
    float mark1, mark2, mark3;
};

int main()
{
    struct StudentData stud[50]
    int pop, i;
    float average;
    printf("Enter number of students enrolled: ");
    scanf("%d", &pop);


    return 0;
}
